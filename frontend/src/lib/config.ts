/**
 * Base API URL, sourced from the Vite environment configuration.
 * See `.env.example` for required variables.
 */
export const API_URL = import.meta.env.VITE_API_URL ?? "http://localhost:4000/api"
